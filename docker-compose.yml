# Main Docker Compose file
# Includes all services: Phoenix (observability), MCP Toolbox, and LibreChat

include:
  - phoenix-compose.yml
  - toolbox-mcp-compose.yml
  - librechat-compose.yml
  # - snowflake-mcp-compose.yml  # Uncomment to use Snowflake Labs MCP (supports key-pair auth)
